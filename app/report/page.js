import ReportPageClient from "./report-page-client";

export const metadata = {
  title: "Report a Problem — BrightListed",
  description: "Experiencing an issue with BrightListed? Check common fixes or send us a report and we'll help you out.",
};

export default function ReportPage() {
  return <ReportPageClient />;
}
